import 'package:flutter/material.dart';

class ShareWindow extends StatefulWidget {
  @override
  _ShareWindow createState() => _ShareWindow();
}

class _ShareWindow extends State<ShareWindow> {
  @override
  Widget build(BuildContext context) {
    return Stack(
      children: <Widget>[],
    );
  }
}
